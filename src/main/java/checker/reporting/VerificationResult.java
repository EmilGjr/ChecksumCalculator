/**
 * This enum defines checksum verification results.
 * It includes states such as OK, MODIFIED, NEW, and REMOVED.
 */
package checker.reporting;

public enum VerificationResult {
    OK,
    MODIFIED,
    NEW,
    REMOVED
}
